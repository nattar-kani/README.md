name: Update dynamic reading log

on:
  workflow_dispatch:
  schedule:
    - cron: "30 0 * * *"

permissions:
  contents: write
*jobs:
  update-reading-log:
    ru*s-on: ubuntu-latest

    steps:
  *   - name: Check out repository
  *     uses: actions/checkout@v4

  *   - name: Set up Python
        u*es: actions/setup-python@v5
      * with:
          python-version: "*.12"

      - name: Update reading*log
        run: python scripts/up*ate_reading_log.py

      - name: *ommit README changes
        shell* bash
        run: |
          if *it diff --quiet README.md; then
  *         echo "No README changes."*            exit 0
          fi

 *        git config user.name "gith*b-actions[bot]"
          git conf*g user.email "41898282+github-acti*ns[bot]@users.noreply.github.com"
*          git add README.md
      *   git commit -m "Update dynamic r*ading log"
          git push
